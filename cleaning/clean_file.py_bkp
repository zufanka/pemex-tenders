# -*- coding: utf-8 -*-

import csv, datetime
from economics import Inflation

#today date
todayDate = datetime.date.today()

#input file
inputfile = open("../pemextest.csv")
pemex = csv.reader(inputfile)

#inflation
inflation = Inflation()

pemex.next()
for row in pemex:

    awardDate = time.strptime(row[5],"%d/%m/%Y")
    pesos = float(row[8])

    inflation.inflate(pesos, datetime.date(todayDate), datetime.date(awardDate), 'Mexico')

    
